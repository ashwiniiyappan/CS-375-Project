<!DOCTYPE html>
<html>
  <style>
    body {
    font-family: Arial, sans-serif;
    background-color: lightblue;
    margin: 0;
    padding: 0;
    }
  </style>
    <head>
        Dragon's Den
    </head>
    <body>
        <% if (watchHistory !== null) { %>
            <ul>
            <% for (let contentId in watchHistory) { %> 
                <li><%= watchHistory[contentId] %></li>
                <a href="/content_view?content_id=<%=watchHistory[contentId] %>">
                    <button id = "content_link">View Page</button>
                </a>
            </ul>
            <% } %>
        <% } else { %>
            <p>No Watch History</p>
        <% } %>   
    </body>
</html>