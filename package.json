{
    "scripts": {
        "start": "cd app && node server.js",
        "setup": "echo $0 && source .env && psql -U $PGUSER -f setup.sql"
    },
    "dependencies": {
        "bcrypt": "^5.1.1",
        "express": "^4.18.2",
        "pg": "^8.11.3"
    }
}
